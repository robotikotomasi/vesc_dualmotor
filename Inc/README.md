# Inc — User header directory

Place project-specific header files (.h) here.  
This directory is added to the compiler include path by `platformio.ini`
via `-IInc`.

**Built-in VESC headers** are located in the project root and in the
subsystem directories (`motor/`, `comm/`, `hwconf/`, etc.) — do not move them.
