/*
 * isr_vector_table.h compatibility
 * ChibiOS maps IRQ handler names to Vector* names.
 * For FreeRTOS/CMSIS we use standard CMSIS names directly.
 * This file is intentionally empty to disable the ChibiOS mappings.
 */

#ifndef ISR_VECTOR_TABLE_COMPAT_H
#define ISR_VECTOR_TABLE_COMPAT_H

/* Standard CMSIS IRQ handler names are used directly.
 * No ChibiOS Vector* mappings needed. */

#endif /* ISR_VECTOR_TABLE_COMPAT_H */
