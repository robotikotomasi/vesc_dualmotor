# Src — User source directory

Place additional application source files (.c / .s) here.  
All `Src/**/*.c` and `Src/**/*.s` files are automatically included in
every build by the `build_src_filter` in `platformio.ini`.

**Built-in VESC sources** live at the project root and in their respective
subsystem directories — do not move them.
